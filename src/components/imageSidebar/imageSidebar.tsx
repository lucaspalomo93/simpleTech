import { COLORS } from "../../constants/colors";

function ImageSidebar() {
  return <div style={{backgroundColor: COLORS.DARK_GREY, height: '100%'}}>ImageSidebar</div>;
}

export default ImageSidebar;
